name: Auto-Convert Data on Push

on:
  push:
    paths:
      - 'public/data.csv'
    branches:
      - master
      - main

jobs:
  convert:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Convert data.csv to products.json
        run: node converter.js public/data.csv public/products.json

      - name: Commit and push if changed
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add public/products.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "chore: auto-generate products.json from data.csv" && git push)
